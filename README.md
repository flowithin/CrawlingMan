how to use

do `make Crawler` then `./Crawler <number of documents you want> <number of threads> > <your log file>`

`list.txt` is the seed list, what you want to crawl from the start

`output.txt` is the final result

